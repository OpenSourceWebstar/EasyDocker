version: "3.8"

networks:
  vpn:
    external: true
    name: vpn

  webmin-proxy:
    image: qoomon/docker-host
    restart: always
    cap_add: ["NET_ADMIN", "NET_RAW"]
    #labels:
      #traefik.enable: true
      #traefik.http.middlewares.webmin-redirect-websecure.redirectscheme.scheme: https
      #traefik.http.routers.webmin-web.rule: Host(`DOMAINSUBNAMEHERE`)
      #traefik.http.routers.webmin-web.entrypoints: web
      #traefik.http.routers.webmin-web.middlewares:
      #traefik.http.routers.webmin-websecure.entrypoints: websecure
      #traefik.http.routers.webmin-websecure.rule: Host(`DOMAINSUBNAMEHERE`)
      #traefik.tags: vpn
      #traefik.docker.network: vpn
      #traefik.http.routers.webmin-websecure.tls: true
      #traefik.http.routers.webmin-websecure.tls.certresolver: production
      #traefik.http.services.webmin-global.loadbalancer.server.port: PORT1
    networks:
      vpn:
        ipv4_address: IPADDRESSHERE
