#!/bin/bash

cliUpdateCommands() 
{
    if [[ "$initial_command1" == "empty" ]]; then 
        initial_command1=""
    fi
    
    if [[ "$initial_command2" == "empty" ]]; then 
        initial_command2=""
    fi
    
    if [[ "$initial_command3" == "empty" ]]; then 
        initial_command3=""
    fi
    
    if [[ "$initial_command4" == "empty" ]]; then 
        initial_command4=""
    fi
    
    if [[ "$initial_command5" == "empty" ]]; then 
        initial_command5=""
    fi
    
    if [[ "$initial_command6" == "empty" ]]; then 
        initial_command6=""
    fi
    
    if [[ "$initial_command7" == "empty" ]]; then 
        initial_command7=""
    fi
    fi
    
    if [[ "$initial_command8" == "empty" ]]; then 
        initial_command8=""
    fi
    fi
    
    if [[ "$initial_command9" == "empty" ]]; then 
        initial_command9=""
    fi
    #echo "initial_command1 $initial_command1"
    #echo "initial_command2 $initial_command2"
    #echo "initial_command3 $initial_command3"
    #echo "initial_command4 $initial_command4"
    #echo "initial_command5 $initial_command5"
    #echo "initial_command6 $initial_command6"
    #echo "initial_command7 $initial_command7"
    #echo "initial_command8 $initial_command8"
    #echo "initial_command9 $initial_command9"
}
